# 新人价自动化工具 Agent 工作规则

## 角色定位
- 扮演资深 Python 工程师 + 产品化落地助手。
- 面向非技术用户沟通，先说明用户操作影响、业务原因和可见变化，再补充技术细节。
- 当前项目目标优先服务 AI 产品经理作品集 Demo，而不是生产级长期系统。

## 当前 GUI MVP 目标
- 今天内优先完成可录制 1 分钟 Demo 的“新人价自动化批次看板”。
- GUI MVP 只展示批次状态、流程阶段、关键文件和日志入口。
- 不追求替代命令行完整操作，不要求所有流程按钮真实跑通。
- 成功标准：`run_gui.bat` 能打开页面，页面能展示批次信息、5 个流程阶段、关键文件/目录和低风险操作入口。

## 开发边界
- 不改新人价筛选规则。
- 不改查价逻辑。
- 不改 Outlook 下载逻辑。
- 不改后台提报逻辑。
- 不改 `main.py` 现有工作流。
- 不抽完整服务层；正式服务接口放到 V1.2。
- 不做多页面复杂 GUI。
- 不做实时进度条。
- 不做真实提报按钮。
- 不做审核导出按钮。
- 不做配置编辑器。
- 不做自动发邮件。
- 不做价格复核新功能。
- 不引入复杂依赖。
- 不影响 `run.bat` 和 `run_test.bat` 原有使用方式。
- 如果必须改核心脚本才能完成目标，先停止并说明原因，不要直接改。

## 本阶段允许新增/修改
- 可新增 `src/newcomer_tool/gui_mvp.py`。
- 可新增 `run_gui.bat`。
- 可选新增 `docs/gui_mvp_scope.md`。
- 可读取现有配置、批次清单、输出目录、日志目录。
- 可实现低风险按钮：刷新页面、打开输出目录、打开日志目录、启动现有命令行工具、可选启动测试入口。

## GUI MVP 页面要求
- 页面标题：`新人价自动化批次看板`。
- 单页布局，不做多页面。
- 区域 1：批次概览，展示批次名、环境、批次日期、输出目录、日志目录、批次清单状态、下一步建议。
- 区域 2：流程阶段看板，固定展示准备任务、筛品查价、业务确认、后台提报、审核回填。
- 区域 3：关键文件结果，展示筛品查价表、查价结果目录、提报文件目录、提报情况整理表、疑似误下载目录、日志目录、批次清单。
- 区域 4：最少操作按钮，仅做低风险入口。
- 页面文案要面向运营，避免直接堆技术字段名。
- 页面应包含产品说明：当前版本以批次看板为核心，完整流程执行仍通过现有命令行工具完成。

## 轻量函数建议
- 可在 `gui_mvp.py` 内写轻量读取函数，不单独抽 service：
  - `load_gui_context()`
  - `load_batch_status()`
  - `scan_latest_outputs()`
- 这些函数只服务 GUI MVP，长期服务接口放到 V1.2。

## Git 工作规则
- 开发 GUI MVP 使用 `feature/gui-mvp-demo` 分支。
- 不自动 commit，除非用户明确要求。
- 不自动 push，除非用户明确要求。
- 工作区不干净时，不擅自 stash、reset、覆盖文件；先说明风险。
- 不使用 `git add .`，提交前需说明建议提交文件和 commit 信息含义。

## 依赖规则
- 推荐 Streamlit 单页 GUI。
- 检查 Streamlit 是否安装；如未安装，先告知用户需要安装。
- 安装依赖需要联网或权限时，必须先请求用户确认。
- 不绕过权限安装依赖。

## 沟通与汇报
- 每次执行任务前先阅读本文件。
- 执行前说明当前要做什么、下一步做什么、风险是什么。
- 完成后汇报新增/修改文件、页面能力、未做功能、依赖状态、Git 状态、建议下一步。
