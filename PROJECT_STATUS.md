# 新人价自动化 Workflow 项目说明

## 1. 项目概览

- **项目名称**：新人价自动化 Workflow
- **项目路径**：`D:\hansiying.1\Desktop\新人价自动化工具`
- **使用方式**：双击 `run.bat`，通过菜单选择模块执行。
- **目标用户**：运营同学。
- **项目目的**：减少新人价活动中筛品、查价、下载、合并、提报等重复人工操作，降低漏处理和重复处理风险。
- **当前阶段**：MVP 主流程已跑通，筛品、查价、邮件下载、最终整合、提报文件生成、后台安全提报均已完成基础可用版本。

## 2. 原流程与现流程

### 原人工流程

1. 人工筛选商品池。
2. 人工整理查价前 SKU。
3. 进入后台上传 SKU 文件并发起查价。
4. 等待邮件返回查价导出结果。
5. 人工下载多个查价结果文件。
6. 用 Excel 合并查价结果。
7. 用 `XLOOKUP` 把 `100天最低价` 回填到筛品结果。
8. 按自营/POP 规则计算新人价。
9. 剔除不符合价格门槛的商品。
10. 发给采销确认是否报名。
11. 根据采销确认表制作提报文件。
12. 进入后台逐个上传并提交报名文件。
13. 后续再从后台导出审核/提报情况并整理。

### 当前自动化流程

1. 工具自动筛品并输出筛品 Excel、SKU 文件和审核日志。
2. 工具自动拆分查价前 SKU 文件。
3. 工具自动进入后台查价工具，上传、查询、批量导出到邮箱。
4. 工具自动从 Outlook 邮件中提取/下载查价结果。
5. 工具自动合并查价结果，并生成最终新人价 Excel。
6. 人工把最终商品结果发给采销确认报名情况。
7. 工具读取采销确认表，自动生成提报 PART 文件。
8. 工具进入新人价补贴报名后台，支持演练、上传不提交、真实提交。

## 3. 当前完成进度

| 模块 | 菜单 | 状态 | 说明 |
| --- | --- | --- | --- |
| 筛品 | `1 筛品` | 已完成 | 读取源货盘，按配置过滤商品，输出筛品结果和审核日志。 |
| 查价前 SKU 拆分 | `2 提取并拆分查价前SKU` | 已完成 | 从筛品输出中提取 SKU，按 5000 条拆分为 PART 文件。 |
| 本地流程串联 | `3 一键执行本地流程` | 已完成 | 串联筛品和查价前 SKU 拆分。 |
| 后台查价上传导出 | `4 后台上传并批量导出到邮箱（实验）` | 已完成 | 复用浏览器登录态，批量上传查价文件、发起查询并导出到邮箱。 |
| Outlook 邮件下载 | `5 Outlook邮件下载（实验）` | 已完成 | 从 Outlook 查找查价邮件并下载附件/结果。 |
| 配置查看 | `6 查看当前配置` | 已完成 | 查看当前 `config.yaml`。 |
| 最终整合 | `7 查价结果整合并生成新人价` | 已完成 | 合并查价结果，回填最低价，计算新人价，输出最终 Excel。 |
| 提报文件生成 | `8 → 1 从采销确认表生成提报文件` | 已完成 | 读取采销确认表，剔除明确退出商品，生成提报 PART 文件。 |
| 后台批量提报 | `8 → 2 后台批量提报文件（安全测试）` | 已完成 | 支持 dry_run、upload_only、submit；已验证连续多文件和全量提交。 |
| 审核/提报情况整理 | 未完成 | 待开发 | 计划处理后台导出的提报/审核情况，并回填到最终整合 Excel。 |

## 4. 菜单入口说明

启动方式：双击 `run.bat`。

主菜单：

```text
1 筛品
2 提取并拆分查价前SKU
3 一键执行本地流程
4 后台上传并批量导出到邮箱（实验）
5 Outlook邮件下载（实验）
6 查看当前配置
7 查价结果整合并生成新人价
8 提报/审核整理
0 退出
```

提报子菜单：

```text
1 从采销确认表生成提报文件
2 后台批量提报文件（安全测试）
0 返回
```

## 5. 模块输入输出流转

### 5.1 筛品模块

- **入口**：菜单 `1 筛品`
- **代码**：`src/newcomer_tool/filtering.py`
- **主要输入**：
  - `config.yaml` 中的 `source_file`
  - 剔除配置：`exclude_category2`、`exclude_name_keywords`、`exclude_erp_accounts`
  - 字段配置：`sku_column`、`product_name_column`、`mapping_column`、`shop_column`、`department_column`、`category2_column`、`erp_column`
- **主要输出**：
  - `data/output/筛品结果/新人价筛品结果_{date_tag}.xlsx`
  - `data/output/筛品结果/筛品结果SKU_{date_tag}.xlsx`
  - `data/output/筛品结果/新人价筛品日志_{date_tag}.txt`
  - `data/output/筛品结果/筛品审核报告_{date_tag}.txt`
- **输出给下一模块**：
  - `筛品结果SKU_{date_tag}.xlsx` 给“查价前 SKU 拆分”使用。
  - `新人价筛品结果_{date_tag}.xlsx` 给“最终整合”使用。

### 5.2 查价前 SKU 拆分模块

- **入口**：菜单 `2 提取并拆分查价前SKU`
- **代码**：`src/newcomer_tool/split_sku.py`
- **主要输入**：
  - 筛品模块输出的 `筛品结果SKU_{date_tag}.xlsx`
  - `config.yaml` 中的 `split_base_name`、`split_chunk_size`、`split_source_sheet`、`split_source_column`
- **主要输出**：
  - 查价前 SKU 汇总文件。
  - 多个查价前 SKU PART 文件，例如：`8月第一批查价前sku_PART01.xlsx`
- **输出给下一模块**：
  - PART 文件给“后台查价上传导出”使用。

### 5.3 后台查价上传导出模块

- **入口**：菜单 `4 后台上传并批量导出到邮箱（实验）`
- **代码**：`src/newcomer_tool/web_steps/backend_upload_export.py`
- **主要输入**：
  - 查价前 SKU PART 文件。
  - `config.yaml` 中的 `backend_url`、`backend_profile_dir`、`backend_wait_after_query`、`backend_wait_after_export`
- **主要输出**：
  - 后台查价任务结果通过系统导出到邮箱。
  - 运行日志保存到 `logs/`。
- **输出给下一模块**：
  - 邮箱中的查价结果邮件给“Outlook 邮件下载”使用。

### 5.4 Outlook 邮件下载模块

- **入口**：菜单 `5 Outlook邮件下载（实验）`
- **代码**：`src/newcomer_tool/web_steps/outlook_download.py`
- **主要输入**：
  - Outlook 邮箱中的查价结果邮件。
  - `config.yaml` 中的 `outlook_url`、`outlook_profile_dir`、`outlook_search`、`outlook_max_mails`
- **主要输出**：
  - 下载后的查价结果 Excel 文件。
  - 通常进入 `data/output/查价结果导出` 或配置指定目录。
- **输出给下一模块**：
  - 多个查价结果 Excel 给“最终整合”使用。

### 5.5 最终整合模块

- **入口**：菜单 `7 查价结果整合并生成新人价`
- **代码**：`src/newcomer_tool/final_pricing.py`
- **主要输入**：
  - 筛品模块输出的 `新人价筛品结果_{date_tag}.xlsx`
  - Outlook 下载的查价结果 Excel
  - `config.yaml` 中的 `final_filter_file`、`final_price_dir`、`final_price_pattern`、`final_price_sku_column`、`final_price_value_column`、`final_price_date_label`
- **处理规则**：
  - 合并多个查价结果文件。
  - 把 `100天最低价` 回填到筛品结果中，字段名为 `xx.xx_查价`。
  - 新建 `新人价` 字段：
    - `1自营3POP_映射` 为自营：`xx.xx_查价 - 8`
    - 非自营：`xx.xx_查价 - 6.5`
    - 计算结果小于等于 0 时填 `0.01`
  - 剔除 `新人价 >= 200` 的商品。
  - 汇总最终商品中的 `销售员ERP帐号`，去重后用英文分号拼接。
- **主要输出**：
  - `data/output/最终结果/新人价最终结果_{date_tag}_{查价日期}.xlsx`
- **输出 Sheet**：
  - `筛选结果`：最终保留商品信息。
  - `查价结果合并`：合并后的查价明细。
  - `ERP汇总`：去重 ERP 列表和英文分号拼接文本。
  - `整合日志`：筛品日志 + 整合日志。
- **输出给下一人工步骤**：
  - `筛选结果` Sheet 给运营整理线上表，并发送采销确认报名情况。

### 5.6 提报文件生成模块

- **入口**：菜单 `8 → 1 从采销确认表生成提报文件`
- **代码**：`src/newcomer_tool/registration_prepare.py`
- **人工前置**：
  - 运营把最终结果中的商品信息整理到线上表。
  - 发给采销确认是否报名。
  - 等待采销回填确认表。
  - 把采销确认表放到 `data/input/提报确认表`。
- **主要输入**：
  - 采销确认表 Excel。
  - 需要包含 SKU、新人价/期望新人价、是否退出/是否报名/是否登记 等字段。
- **报名判断规则**：
  - 字段为 `是否退出` 时：空白默认参加。
  - `是否退出` 填 `退出`、`是`、`不参加`、`不报名`、`不提报`、`取消`、`放弃` 时剔除。
  - 字段为 `是否报名/是否登记` 时：空白默认参加；明确否定才剔除。
- **主要输出**：
  - `data/output/提报文件/新人价提报.xlsx`
  - `data/output/提报文件/新人价提报_PART01.xlsx`、`PART02.xlsx` 等
- **输出规则**：
  - 提报文件列名为 `skuid`、`促销价`。
  - 每个 PART 默认最多 5000 条。
- **输出给下一模块**：
  - 提报 PART 文件给“后台批量提报”使用。

### 5.7 后台批量提报模块

- **入口**：菜单 `8 → 2 后台批量提报文件（安全测试）`
- **代码**：`src/newcomer_tool/web_steps/registration_submit.py`
- **主要输入**：
  - `data/output/提报文件/新人价提报_PART*.xlsx`
  - `config.yaml` 中的 `registration_url`、`registration_profile_dir`、`registration_month_threshold_day`
- **运行模式**：
  - `dry_run`：只演练到上传页，不上传、不提交。
  - `upload_only`：选择文件上传到页面，但不点击提交。
  - `submit`：真实上传并点击提交，必须输入 `确认提报`。
- **安全机制**：
  - 默认推荐只跑 1 个文件。
  - 可选择从第几个 PART 文件开始，避免重复提交。
  - 全量执行必须输入 `全量提报`。
  - 真实提交必须输入 `确认提报`。
  - 找不到明确按钮会停止并保存截图/HTML，不做模糊提交。
- **已验证能力**：
  - 可从指定 PART 序号开始提交。
  - 可连续提交多个文件。
  - 可全量提交剩余文件。
  - 提交成功后会回到 `新人补贴报名`，重新找 `首单新人价` 卡片并点击 `前往报名`。
- **主要输出**：
  - 后台报名提交结果。
  - 日志：`logs/新人价提报自动上传日志.txt`
  - 调试文件：`logs/debug_registration_submit/`

## 6. 人工操作边界

当前仍保留人工处理的部分：

1. **采销确认报名**
   - 工具生成最终新人价 Excel 后，运营需要把最终商品信息整理到线上表。
   - 发给采销确认是否报名。
   - 等待采销回填确认表。

2. **提报前风险确认**
   - 真实提交前建议先用 `dry_run` 和 `upload_only` 验证页面路径和文件识别。
   - 真实提交仍需要人工在菜单中输入 `确认提报`。

3. **异常页面人工判断**
   - 如果后台页面改版、按钮消失、网络卡顿，脚本会保存截图和 HTML。
   - 需要人工查看 `logs/debug_registration_submit/` 后再决定是否调整脚本。

4. **后台审核/提报结果导出**
   - 当前还未自动整理后台审核情况。
   - 后续计划开发新模块处理。

## 7. 目录约定

```text
新人价自动化工具/
├─ run.bat                         # 启动菜单
├─ config.yaml                     # 当前配置
├─ config.example.yaml             # 配置示例
├─ README.md                       # 使用说明
├─ PROJECT_STATUS.md               # 本文档，项目进度和交接说明
├─ src/newcomer_tool/              # Python 主代码
│  ├─ main.py                      # 菜单入口
│  ├─ filtering.py                 # 筛品模块
│  ├─ split_sku.py                 # SKU 拆分模块
│  ├─ final_pricing.py             # 最终整合模块
│  ├─ registration_prepare.py      # 提报文件生成模块
│  └─ web_steps/                   # 浏览器/网页自动化脚本
├─ data/input/                     # 人工输入文件目录
│  └─ 提报确认表/                  # 采销确认表建议放这里
├─ data/output/                    # 工具输出目录
│  ├─ 筛品结果/
│  ├─ 查价结果导出/
│  ├─ 最终结果/
│  └─ 提报文件/
├─ browser_profiles/               # 浏览器登录态缓存
└─ logs/                           # 日志和调试截图
```

## 8. 推荐日常运行顺序

### 完整流程

1. 更新 `config.yaml` 中的源文件和日期配置。
2. 菜单 `1 筛品`。
3. 菜单 `2 提取并拆分查价前SKU`。
4. 菜单 `4 后台上传并批量导出到邮箱（实验）`。
5. 等待邮件返回。
6. 菜单 `5 Outlook邮件下载（实验）`。
7. 菜单 `7 查价结果整合并生成新人价`。
8. 人工把最终商品表发采销确认。
9. 菜单 `8 → 1 从采销确认表生成提报文件`。
10. 菜单 `8 → 2 后台批量提报文件（安全测试）`。
11. 先 `dry_run`，再 `upload_only`，最后按需 `submit`。

### 提报续跑建议

如果已经成功提交了部分 PART 文件：

- 已提交 `PART01`：下一次起始序号输入 `2`。
- 已提交 `PART01` 到 `PART05`：下一次起始序号输入 `6`。
- 先跑 1 个文件确认页面正常，再选择从起始位置跑到最后。

## 9. 当前已知风险与注意事项

1. **后台页面依赖登录态**
   - 浏览器登录态保存在 `browser_profiles/`。
   - 如果登录过期，需要人工重新登录。

2. **后台页面结构可能变化**
   - 页面按钮、tab、iframe 结构变化会影响自动点击。
   - 脚本目前会保存失败截图和 HTML，便于定位。

3. **真实提交不可随意测试**
   - `submit` 会真实点击后台提交。
   - 调试时优先使用 `dry_run` 或 `upload_only`。

4. **提报文件不可重复提交**
   - 如果某个 PART 已经提交成功，续跑时必须从下一个序号开始。
   - 菜单已支持输入起始文件序号。

5. **采销确认表字段名可能不统一**
   - 当前已适配 SKU、新人价/期望新人价、是否退出/是否报名/是否登记。
   - 如果新表头变化，需要扩展 `registration_prepare.py` 的字段识别逻辑。

## 10. 下一步开发计划

### 优先级 P0

1. **后台审核/提报情况整理模块**
   - 输入：后台导出的审核情况/提报情况 Excel。
   - 输出：回填到最终整合 Excel，增加提报状态、审核状态、失败原因等字段。
   - 目标：形成从筛品到提报结果回收的闭环。

2. **提报结果去重与防重复提交机制**
   - 记录已提交的 PART 文件或 SKU 清单。
   - 再次运行时提示哪些文件可能已经提交过。
   - 避免误重复提交。

### 优先级 P1

1. **增强日志汇总**
   - 每个模块生成统一格式运行摘要。
   - 汇总成功数量、失败数量、跳过数量、输出文件路径。

2. **配置项可视化检查**
   - 启动时检查关键路径是否存在。
   - 对缺失文件、空目录、字段配置异常给出更明确提示。

3. **提报后成功提示识别优化**
   - 当前部分页面提交成功后提示不稳定，只能保存截图供人工确认。
   - 后续可基于页面跳转 URL、接口响应或提报列表变化做更准确判断。

### 优先级 P2

1. **减少浏览器自动化脆弱性**
   - 若能找到后台接口，可考虑替代部分页面点击。
   - 降低页面改版对工具影响。

2. **一键总流程编排**
   - 在安全确认足够完善后，把查价、下载、整合、提报前准备进一步串联。
   - 保留真实提交前的人工强确认。

## 11. 当前交接结论

- 本项目已完成新人价运营流程中最耗时的筛品、查价、下载、整合、提报文件生成和后台提报自动化。
- 目前主要人工节点集中在采销确认和异常结果复核。
- 下一阶段重点是开发“后台审核/提报情况导出整理”模块，并补强防重复提交和提报结果记录能力。
