下面是基于我已读取的 `D:\hansiying.1\Desktop\新人价自动化工具` 当前脚本整理出的报告版盘点。定位角度：资深开发工程师 + AI PM，重点看“现有能力、人工依赖、输入输出、失败表现、GUI 化障碍、模块拆分”。

**结论摘要**

- 当前脚本已经覆盖“筛品 → 查价 → 整合 → 业务确认 → 提报 → 审核回填 → 复提判断”的完整业务闭环。
- 目前形态更像“命令行自动化工具 + 若干网页自动化脚本”，还不是可直接给 GUI 调用的服务层。
- 最大问题不是能力缺失，而是职责耦合：`main.py` 负责菜单、确认、编排、子进程调用；网页自动化脚本也混合了 CLI 参数、浏览器动作、日志和业务判断。
- GUI 化前建议先拆出：工作流层、领域规则层、IO 层、自动化层、邮件层，并统一返回结构化结果。

---


## 1. 当前脚本能力总览

### 1.1 当前能完成的主流程

现有脚本可以完成以下步骤：

|阶段|能力|当前自动化程度|
|---|---|---|
|0|配置读取、批次命名、输出目录管理|基本自动|
|1|大盘订单源表筛品|基本自动|
|2|查价前 SKU 提取和拆分|自动|
|3|后台查价上传并批量导出到邮箱|半自动，依赖登录态和页面稳定|
|4|Outlook 邮件读取和查价结果下载|半自动，依赖 Outlook 客户端和邮件到达|
|5|查价结果整合并计算新人价|自动|
|6|业务/采销确认表生成提报文件|基本自动，但确认表需人工提供|
|7|后台新人价补贴报名|半自动，高风险动作保留人工确认|
|8|后台提报/审核情况导出|半自动，依赖后台和 Outlook|
|9|提报情况整理并回填最终结果|自动|
|10|下一轮复提候选识别|自动，依赖上一轮提报整理表|

主入口在 `src/newcomer_tool/main.py:656`，工作流向导在 `src/newcomer_tool/main.py:630`。

---

## 2. 现有脚本能力地图

## 2.1 配置与批次管理

### 相关文件

- `src/newcomer_tool/config.py:82`
- `src/newcomer_tool/batch.py:21`
- `config.yaml`
- `config.test.yaml`
- `config.example.yaml`

### 能力

|能力|说明|
|---|---|
|配置读取|读取轻量 YAML 配置，支持默认值覆盖|
|正式/测试切换|`run.bat` 使用正式配置，`run_test.bat` 使用 `config.test.yaml`|
|批次命名|基于 `batch_name`、`batch_date` 生成文件名|
|输出目录管理|管理筛品、查价前 SKU、查价结果、最终结果、提报文件、审核导出等目录|
|文件防覆盖|输出文件重名时自动追加序号|
|批次清单|用 JSON 记录每个阶段的关键产物|

### 输入

- `config.yaml` 或 `config.test.yaml`
- 环境变量 `NEWCOMER_TOOL_CONFIG`
- 批次配置：`batch_name`、`batch_date`
- 路径配置：输入目录、输出目录、日志目录、浏览器 profile 目录等

### 输出

- 统一的 `AppConfig`
- 批次名、批次日期
- 批次清单 JSON
- 各阶段输出路径

### 失败会怎样

|失败点|当前表现|
|---|---|
|配置文件不存在|使用默认配置继续运行|
|配置项写错|大多数情况下到具体步骤才报错|
|路径不存在|具体步骤创建输出目录，输入目录/文件不存在则报错|
|YAML 复杂写法|当前轻量解析器不支持完整 YAML 语法，可能解析不符合预期|

### 人工依赖

- 需要人工维护 `config.yaml` 中的路径、批次名、文件匹配规则。
- 当前没有图形化配置页，也没有统一配置校验页。

---

## 2.2 主菜单与工作流编排

### 相关文件

- `src/newcomer_tool/main.py:656`
- `src/newcomer_tool/main.py:532`
- `src/newcomer_tool/main.py:582`
- `src/newcomer_tool/main.py:611`
- `src/newcomer_tool/main.py:476`

### 当前菜单能力

主菜单包含：

|菜单|能力|
|---|---|
|1|筛品|
|2|提取并拆分查价前 SKU|
|3|一键执行本地流程|
|4|后台上传并批量导出到邮箱|
|5|Outlook 邮件下载|
|6|查看当前配置|
|7|查价结果整合并生成新人价|
|8|提报/审核整理|
|9|新人价工作流向导|
|0|退出|

提报/审核子菜单包含：

|菜单|能力|
|---|---|
|1|从采销确认表生成提报文件|
|2|后台批量提报文件|
|3|后台导出提报/审核情况|
|4|整理提报情况并回填最终结果|

### 输入

- 用户在命令行输入菜单编号
- 用户输入确认语
- 用户输入业务确认表路径
- 配置文件

### 输出

- 控制台提示
- 调用各阶段脚本
- 更新批次清单
- 打印阶段结果

### 失败会怎样

- 大多数异常在 `main()` 中被捕获并打印 `执行失败：xxx`。
- 部分子流程失败后会停止后续流程。
- 后台自动化失败时通常会保存日志、截图或 HTML 快照。

### 人工依赖

- 需要用户在黑窗口中选择菜单。
- 多个关键步骤需要用户输入 `y` 或确认语。
- 工作流中间会让用户决定是否继续下一步。

### GUI 化问题

`main.py` 不能直接作为 GUI 后端调用，因为它大量使用：

- `input()`
- `print()`
- `pause()`
- 人工确认
- 子进程调用
- 阻塞式流程
- 非结构化异常输出

GUI 需要的是可调用函数，例如：

```
run_stage_1(config) -> StageResult
run_registration_prepare(config, source_file) -> StageResult
run_status_merge(config) -> StageResult
```

而不是菜单驱动的 `input()` 流程。

---

# 3. 阶段能力明细

## 3.1 阶段 1：筛品

### 相关文件

- `src/newcomer_tool/filtering.py:486`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|源表识别|支持配置固定源表，也支持自动检测最新大盘源表|
|复提判断|没有新源表时，可从最近提报整理表生成复提候选|
|字段校验|校验 SKU、商品名、映射、店铺、部门、类目、ERP、指标列等|
|部门筛选|保留医药、营养保健、医疗器械、消费器械等目标部门|
|自营/POP 筛选|保留自营和指定 POP 店铺关键词，例如京喜|
|类目剔除|剔除健康服务套餐、其他服务、特殊商品等|
|SKU 去重|按 SKU 去重，并对配置指标列求和|
|名称关键词剔除|剔除非卖品、赠品等|
|历史状态过滤|普通成功剔除，失败和特定成功失败原因进入复提|
|ERP 剔除|剔除配置中的指定 ERP|
|输出审计|生成过程日志和审核记录|

### 输入

|输入|来源|
|---|---|
|大盘订单源表|人工下载后放入配置目录，或配置 `source_file`|
|历史提报整理表|上一轮阶段 4 输出|
|筛品规则配置|`config.yaml`|
|字段名配置|`config.yaml`|

### 输出

|输出|用途|
|---|---|
|筛选结果 Excel|后续查价整合和业务确认基础|
|筛品 SKU 文件|拆分查价前 SKU|
|过程日志|排查筛品过程|
|审核记录|判断字段缺失、剔除比例异常等|
|可选 CSV|兼容其他工具|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|找不到源表且没有复提表|抛出 `FileNotFoundError`|
|关键字段缺失|生成筛品失败过程记录，并终止|
|字段名不匹配|终止或进入预警，取决于字段重要性|
|源表为空|后续筛选结果为空或报错|
|剔除比例异常|写入审计预警，不一定中断|

### 人工依赖

- 初始大盘源表仍需人工从后台/取数中心下载。
- 需要人工维护源表目录、文件匹配规则。
- 字段变化后需要人工改配置或脚本。
- 筛品结果是否合理仍需人工抽查。

### GUI 化问题

- 筛品函数本身可以改造成 GUI 后端调用。
- 但当前日志主要是文本列表，不是统一的结构化事件。
- 字段缺失、预警、失败没有统一错误码。
- GUI 需要额外封装：进度、警告、失败原因、输出文件列表。

---

## 3.2 阶段 1：查价前 SKU 拆分

### 相关文件

- `src/newcomer_tool/split_sku.py:59`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|找最新筛品结果|可自动定位最新筛品输出|
|提取 SKU|从指定 Sheet 和 SKU 列提取|
|拆分文件|按 `split_chunk_size` 拆分，默认 5000|
|生成汇总|输出完整 SKU 汇总文件|
|生成 PART|输出多个待上传 PART 文件|

### 输入

|输入|来源|
|---|---|
|筛品结果 Excel|阶段 1 筛品输出|
|Sheet 名|配置项 `split_source_sheet`|
|SKU 列名|配置项 `split_source_column`|
|拆分大小|配置项 `split_chunk_size`|

### 输出

|输出|用途|
|---|---|
|查价 SKU 汇总文件|留档|
|查价 SKU PART 文件|后台查价上传|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|找不到筛品结果|抛出文件不存在错误|
|Sheet 不存在|报错|
|SKU 列不存在|报错|
|SKU 为空|可能输出为空或报错|

### 人工依赖

- 通常不需要人工操作。
- 如果自动识别错文件，需要人工指定或清理目录。

### GUI 化问题

- 基本适合 GUI 调用。
- 需要将返回结果结构化展示：SKU 数、PART 数、文件路径。

---

## 3.3 阶段 1：后台查价上传并导出

### 相关文件

- `src/newcomer_tool/web_steps/backend_upload_export.py:511`
- `src/newcomer_tool/main.py:136`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|启动浏览器|使用 Playwright|
|复用登录态|使用 `browser_profiles/backend`|
|进入查价页面|打开后台新人价查价 URL|
|处理内嵌登录|检测 ERP 登录状态|
|选择查价功能|点击最低价查询、批量上传等|
|上传文件|上传查价前 SKU PART|
|等待结果|等待查询完成|
|批量导出|点击批量导出到邮箱|
|日志记录|写后台查价日志|
|调试快照|异常时保存截图/HTML|

### 输入

|输入|来源|
|---|---|
|查价 SKU PART 文件|拆分步骤输出|
|后台 URL|配置项 `backend_url`|
|浏览器登录缓存|配置项 `backend_profile_dir`|
|文件匹配规则|配置项和菜单输入|
|等待时间|`backend_wait_after_query`、`backend_wait_after_export`|

### 输出

|输出|用途|
|---|---|
|后台导出邮件|后续 Outlook 下载|
|日志文件|排查后台查价|
|截图/HTML|页面异常排查|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|未安装 Playwright|抛出安装提示|
|登录态失效|停在登录或等待人工登录|
|页面 loading 卡住|超时、保存快照|
|找不到按钮|保存调试截图/HTML 后失败|
|上传文件不存在|抛出文件不存在错误|
|查询无结果|等待超时或结果状态异常|

### 人工依赖

- 首次运行需要人工登录后台。
- 页面卡住时可能需要人工刷新。
- 页面结构变化时需要人工维护脚本。
- 导出是否真的成功，需要通过邮件或后续下载确认。

### GUI 化问题

这个脚本目前不适合直接给 GUI 调用，原因：

- 是 CLI 脚本，使用 `argparse`。
- 通过子进程从 `main.py` 调用。
- 运行时间长，缺少结构化进度事件。
- 依赖真实浏览器窗口和登录态。
- 失败信息主要在日志和截图，不是统一返回对象。
- 页面自动化动作和业务流程耦合。

GUI 化前建议包装成：

```
price_query_export_service.run(files, options, progress_callback) -> AutomationResult
```

---

## 3.4 阶段 1：Outlook 查价结果下载

### 相关文件

- `src/newcomer_tool/web_steps/outlook_download.py:348`
- `src/newcomer_tool/main.py:187`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|读取 Outlook 邮件|通过 PowerShell 调用 Outlook 客户端|
|筛选邮件|按主题、发件人、未读/已读等筛选|
|提取链接|从邮件 HTML/文本中提取下载链接|
|批次过滤|支持按时间窗口筛选最近一批|
|下载文件|根据链接下载 Excel|
|标记已读|可选处理成功后标记邮件已读|
|生成链接报告|输出链接提取报告|

### 输入

|输入|来源|
|---|---|
|Outlook 本地客户端|用户电脑环境|
|邮件主题关键词|配置或参数|
|发件人关键词|配置或参数|
|URL 关键词|默认 `.xlsx`|
|下载目录|配置项 `download_output_dir`|
|最大扫描邮件数|配置项或参数|

### 输出

|输出|用途|
|---|---|
|查价结果 Excel|最终整合|
|Outlook 下载日志|排查邮件读取和下载|
|链接报告|校验提取到哪些链接|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|Outlook 未登录|PowerShell 读取失败或无邮件|
|邮件未到达|找不到匹配邮件|
|邮件主题/发件人变化|找不到链接|
|下载链接过期|下载失败|
|网络问题|下载失败并记录日志|
|文件重名|自动生成唯一文件名|

### 人工依赖

- Outlook 客户端需要本机可用且已登录。
- 邮件到达延迟时需要等待或重试。
- 主题/发件人变化时需要人工改配置。
- 邮件筛选结果需要必要时人工确认。

### GUI 化问题

- 当前依赖 PowerShell + Outlook COM，不是纯 Python 服务。
- 读取、筛选、下载、标记已读混在一个 CLI 脚本里。
- GUI 需要展示“正在等邮件”“已扫描 N 封”“找到 M 个链接”等结构化状态。

---

## 3.5 阶段 1：查价结果整合与新人价计算

### 相关文件

- `src/newcomer_tool/final_pricing.py:261`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|找筛品结果|自动取最新或使用配置指定|
|找查价文件|从查价结果目录按 pattern 读取|
|合并查价结果|多个 Excel 合并|
|字段识别|识别 SKU 列、价格列|
|SKU 匹配|将查价价格回填到筛品结果|
|计算新人价|自营减 8，非自营减 6.5|
|价格兜底|小于等于 0 时置为 0.01|
|二次剔除|新人价大于等于 200 剔除|
|ERP 汇总|生成去重销售员 ERP 汇总文本|
|报表输出|输出最终筛品查价表|

### 输入

|输入|来源|
|---|---|
|筛品结果 Excel|筛品步骤输出|
|查价结果 Excel|Outlook 下载|
|SKU 字段配置|`final_price_sku_column`|
|价格字段配置|`final_price_value_column`|
|日期标签|`final_price_date_label` 或自动生成|

### 输出

|输出|用途|
|---|---|
|筛品查价表|业务确认基础|
|查价结果合并 Sheet|留档与排查|
|ERP 汇总 Sheet|发业务确认邮件|
|整合日志 Sheet|追溯处理过程|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|找不到筛品结果|报错|
|找不到查价文件|报错|
|查价字段缺失|报错|
|价格无法解析|对应 SKU 无有效价格或日志提示|
|部分 SKU 未匹配|不终止，记录未匹配数量|
|输出文件被占用|生成新文件名或保存失败，取决于具体占用方式|

### 人工依赖

- 业务上需要人工检查最终筛品查价表。
- 需要人工将结果迁移到线上表。
- 需要人工发起业务/采销确认。

### GUI 化问题

- 核心函数较适合 GUI 调用。
- 但 Excel 文件读写耗时需要进度提示。
- 价格规则应抽成独立规则模块，便于 GUI 展示和配置。

---

## 3.6 阶段 2：业务确认表生成提报文件

### 相关文件

- `src/newcomer_tool/registration_prepare.py:136`
- `src/newcomer_tool/main.py:582`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|识别业务确认表|自动取最新或用户输入路径|
|表头识别|从 Excel 中识别表头行|
|字段识别|识别 SKU、新人价、是否退出/是否参加等字段|
|剔除不提报 SKU|命中退出、不参加、不报名、不提报、取消、放弃等关键词时剔除|
|生成汇总|生成提报 SKU 汇总文件|
|拆分 PART|按 `registration_chunk_size` 拆分|
|写日志|输出生成日志|

### 输入

|输入|来源|
|---|---|
|业务/采销确认表|人工回收后放入目录|
|提报输出目录|`registration_submit_dir`|
|拆分大小|`registration_chunk_size`|
|批次名和日期|`batch_name`、`batch_date`|

### 输出

|输出|用途|
|---|---|
|提报 SKU 汇总文件|留档|
|提报 SKU PART 文件|后台报名上传|
|生成日志|排查确认表解析|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|找不到确认表|取消或报错|
|表头识别失败|报错|
|SKU 字段缺失|报错|
|新人价字段缺失|报错|
|没有可提报 SKU|报错并提示检查字段|
|输出目录不存在|自动创建|

### 人工依赖

- 业务确认表需要人工准备。
- 确认表格式变化时，需要人工维护字段识别逻辑。
- 剔除规则是否合理，需要业务确认。

### GUI 化问题

- 核心函数较适合 GUI 调用。
- 但需要 GUI 提供文件选择器、字段预览、剔除数量预览。
- 应返回“总行数、剔除数、可提报数、PART 文件列表”等结构化数据。

---

## 3.7 阶段 3：后台新人价补贴报名

### 相关文件

- `src/newcomer_tool/web_steps/registration_submit.py:932`
- `src/newcomer_tool/main.py:275`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|启动浏览器|使用 Playwright|
|复用登录态|使用后台 profile|
|进入新人价报名页|打开配置 URL|
|识别月份计划|根据当前日期和阈值识别本月/下月计划|
|进入报名|点击对应计划报名|
|进入批量上传|找到批量上传区域|
|上传提报文件|上传指定 PART 文件|
|可选提交|根据模式决定是否真实提交|
|安全控制|`dry_run`、`upload_only`、`submit` 三种模式|
|调试快照|失败时保存截图/HTML|

### 输入

|输入|来源|
|---|---|
|提报 PART 文件|阶段 2 输出|
|文件匹配规则|`registration_submit_pattern`|
|后台报名 URL|`registration_url`|
|登录 profile|`registration_profile_dir`|
|运行模式|菜单选择或参数|
|提交确认语|`确认提报`|
|起始序号和 limit|用户输入或默认|

### 输出

|输出|用途|
|---|---|
|后台提报动作结果|后续审核导出|
|提报日志|排查上传/提交|
|调试截图/HTML|页面异常排查|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|登录态失效|停在登录页或等待登录|
|月份计划识别失败|报错并保存调试信息|
|找不到报名按钮|报错并保存快照|
|找不到上传区|报错|
|上传失败|报错|
|真实提交未传确认语|阻止提交|
|页面反馈异常|等待超时或保存快照|

### 人工依赖

- 首次登录需要人工。
- 真实提报必须人工确认。
- 默认限制只处理少量文件，防止误全量。
- 页面变化需要人工维护定位逻辑。
- 提交结果仍建议人工抽查后台。

### GUI 化问题

这是最不适合直接 GUI 调用的部分之一：

- CLI 参数驱动。
- 高风险真实提交。
- 强依赖浏览器窗口和页面状态。
- 缺少统一状态机。
- 缺少可恢复任务 ID。
- 失败后依赖日志和截图人工判断。
- 不应在 GUI 主线程同步阻塞运行。

GUI 化建议设计为后台任务：

```
RegistrationSubmitTask
- task_id
- mode: dry_run / upload_only / submit
- files
- current_file
- status
- progress
- requires_user_confirmation
- screenshots
- logs
- result
```

---

## 3.8 阶段 4：后台审核/提报情况导出

### 相关文件

- `src/newcomer_tool/web_steps/registration_status_export.py:407`
- `src/newcomer_tool/main.py:390`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|进入已报名页|打开后台新人价报名页|
|选择月份计划|识别本月/下月计划|
|点击查看|进入已报名详情|
|点击下载|触发后台导出|
|等待邮件|等待 Outlook 收到导出邮件|
|下载 Excel|下载提报情况文件|
|支持只下载最新邮件|不进后台，直接下载最新匹配邮件|

### 运行模式

|模式|行为|
|---|---|
|`dry_run`|到下载页，不点击下载|
|`export_only`|点击后台下载，不抓邮件|
|`export_and_download`|点击下载并等待 Outlook 邮件|
|`download_latest_only`|只下载最新匹配邮件|

### 输入

|输入|来源|
|---|---|
|后台 URL|`registration_url`|
|登录 profile|`registration_profile_dir`|
|下载目录|`registration_status_export_dir`|
|邮件主题|`registration_status_mail_subject`|
|邮件发件人|`registration_status_mail_sender`|
|邮件等待时间|`registration_status_mail_timeout`|
|导出确认语|`确认导出`|

### 输出

|输出|用途|
|---|---|
|提报情况导出 Excel|阶段 4 回填|
|导出日志|排查后台导出|
|下载日志|排查 Outlook 下载|
|调试快照|页面异常排查|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|找不到已报名页|报错|
|找不到月份计划|报错并保存快照|
|找不到下载按钮|报错并保存快照|
|未传确认语|阻止点击下载|
|邮件超时未到|超时失败|
|下载链接失效|下载失败|

### 人工依赖

- 首次登录后台需要人工。
- 点击下载前需要人工确认。
- 邮件延迟可能需要人工等待或重试。
- 页面结构变化需要人工维护脚本。

### GUI 化问题

- 和后台提报一样，应作为异步任务运行。
- 需要将“触发导出”和“等待邮件下载”拆成两个可恢复步骤。
- 当前脚本同时依赖后台自动化和 Outlook 下载，GUI 调用前建议解耦。

---

## 3.9 阶段 4：提报情况整理并回填最终结果

### 相关文件

- `src/newcomer_tool/registration_status_merge.py:431`

### 当前能完成哪些步骤

|步骤|说明|
|---|---|
|自动找输入文件|找提报情况导出、最终结果、确认表|
|读取提报记录|识别 SKU、提报状态、详情、促销状态、时间字段|
|失败原因分类|对常见失败原因打标签|
|同 SKU 归并|多条记录成功优先，否则取最新失败|
|重复提报处理|详情含严重错误时标记重复提报，并视为成功|
|回填最终结果|增加提报状态、标签、详情、记录数等字段|
|读取采销退出|从确认表回填采销确认退出状态|
|生成汇总 Sheet|原始、归并、汇总、日志|
|输出新文件|不覆盖原最终结果|

### 输入

|输入|来源|
|---|---|
|提报情况导出 Excel|审核导出步骤输出|
|最终结果 Excel|查价整合步骤输出|
|业务确认表|阶段 2 输入|
|字段识别规则|脚本内候选字段|

### 输出

|输出|用途|
|---|---|
|提报情况整理表|当前批次最终归档|
|回填后的筛选结果 Sheet|查看每个 SKU 最终提报状态|
|提报情况原始 Sheet|留存后台导出原始数据|
|提报情况归并 Sheet|查看同 SKU 归并结果|
|提报汇总 Sheet|看成功、失败、未匹配分布|
|提报整理日志 Sheet|排查处理过程|
|下一轮复提依据|筛品阶段可读取该整理表|

### 失败会怎样

|失败场景|当前表现|
|---|---|
|找不到提报情况文件|报错|
|找不到最终结果文件|报错|
|找不到确认表|报错|
|缺少 `筛选结果` Sheet|报错|
|SKU 字段识别失败|报错|
|提报状态字段识别失败|报错|
|无有效 SKU|报错|
|输出文件被占用|可能保存失败或自动生成唯一名，取决于路径生成阶段|

### 人工依赖

- 通常不需要人工干预。
- 但需要人工确认自动识别的三个输入文件是否正确。
- 失败标签规则变化时，需要维护脚本。

### GUI 化问题

- 核心逻辑适合 GUI 调用。
- 但应拆出“状态读取”“状态归并”“Excel 回填”三个部分。
- 需要返回结构化汇总：成功数、失败数、重复提报数、未匹配数、各失败标签数量。

---

# 4. 哪些步骤还依赖人工放文件、改配置、看日志

## 4.1 人工放文件

|步骤|需要人工放什么|当前原因|
|---|---|---|
|筛品|大盘订单源表|源表来自后台/取数中心，当前未全自动下载|
|业务确认|业务/采销确认表|需要业务判断是否参加、是否退出、确认价格|
|提报回填|必要时手动指定提报情况文件、最终结果文件、确认表|自动取最新可能取错，尤其目录里有多个批次文件时|

## 4.2 人工改配置

|配置类型|例子|风险|
|---|---|---|
|批次配置|`batch_name`、`batch_date`|文件命名、批次清单错误|
|源表检测|`source_auto_dir`、`source_auto_patterns`|找不到源表或找错源表|
|字段名配置|SKU、商品名、部门、ERP、价格列|字段变更会导致报错或误处理|
|筛品规则|目标部门、剔除类目、剔除关键词|业务规则变化需人工维护|
|网页参数|后台 URL、profile 目录、等待时间|页面变化或登录态变化会失败|
|邮件参数|主题、发件人、最大扫描数、等待时间|邮件模板变化会找不到文件|
|提报参数|chunk size、文件 pattern、月份阈值|提报文件匹配或计划月份可能错误|

## 4.3 人工看日志

|场景|需要看什么|
|---|---|
|筛品字段缺失|筛品失败过程记录、审核记录|
|筛品结果异常|筛品日志、剔除数量|
|后台查价失败|后台查价日志、截图、HTML|
|Outlook 没下载到|Outlook 下载日志、链接报告|
|提报上传失败|提报日志、页面截图|
|审核导出失败|导出日志、截图、邮件等待日志|
|提报状态异常|提报整理日志、提报情况归并 Sheet|

---

# 5. 哪些地方不能直接给 GUI 调用

## 5.1 不能直接调用的部分

|模块|原因|
|---|---|
|`src/newcomer_tool/main.py:656`|菜单式 `input()` / `print()` / `pause()`，不是服务函数|
|`src/newcomer_tool/main.py:136`|通过子进程调用后台查价脚本，缺少结构化进度|
|`src/newcomer_tool/main.py:187`|通过子进程调用 Outlook 下载脚本，结果主要靠日志和文件|
|`src/newcomer_tool/main.py:275`|提报流程包含人工确认、CLI 输入、子进程|
|`src/newcomer_tool/main.py:390`|审核导出包含后台自动化和 Outlook 等待，运行时间长|
|`src/newcomer_tool/web_steps/backend_upload_export.py:511`|CLI 脚本，直接控制浏览器，有阻塞和环境依赖|
|`src/newcomer_tool/web_steps/outlook_download.py:348`|CLI 脚本，依赖 Outlook COM/PowerShell|
|`src/newcomer_tool/web_steps/registration_submit.py:932`|高风险真实提交，必须封装任务状态和确认机制|
|`src/newcomer_tool/web_steps/registration_status_export.py:407`|同时依赖后台页面和邮件轮询，不适合 GUI 同步调用|

## 5.2 可以较容易改造成 GUI 后端的部分

|模块|GUI 化难度|原因|
|---|---|---|
|`src/newcomer_tool/filtering.py:486`|中|业务逻辑清晰，但需结构化日志和错误码|
|`src/newcomer_tool/split_sku.py:59`|低|输入输出明确，副作用仅文件写入|
|`src/newcomer_tool/final_pricing.py:261`|中低|主要是 Excel 读写和规则计算|
|`src/newcomer_tool/registration_prepare.py:136`|中低|适合文件上传/选择后调用|
|`src/newcomer_tool/registration_status_merge.py:431`|中|逻辑适合服务化，但需拆读写和归并规则|

## 5.3 GUI 调用前必须补的能力

|能力|说明|
|---|---|
|结构化返回值|每步返回 `status`、`message`、`outputs`、`warnings`、`errors`|
|统一错误码|例如 `SOURCE_NOT_FOUND`、`COLUMN_MISSING`、`LOGIN_REQUIRED`|
|进度事件|长任务需要进度回调或事件流|
|任务状态机|后台查价、提报、审核导出应异步执行|
|可恢复任务|邮件等待、页面失败后可重试|
|配置校验|GUI 启动前检测路径、字段、登录态、依赖|
|日志聚合|把日志文件、截图、HTML 汇总到任务诊断包|
|人工确认节点|真实提交、确认导出必须由 GUI 显示强确认|

---

# 6. 模块拆分建议

## 6.1 推荐目标分层

建议目标结构：

```
src/newcomer_tool/
  cli/
    main.py
    menus.py
    prompts.py

  workflows/
    stage1_pricing.py
    registration_prepare_flow.py
    registration_submit_flow.py
    registration_status_flow.py

  core/
    config.py
    batch.py
    paths.py
    logging.py
    columns.py
    sku.py
    result.py
    errors.py

  io/
    excel_reader.py
    excel_writer.py
    csv_reader.py
    reports.py

  domain/
    filtering/
      rules.py
      resubmit.py
      service.py
    pricing/
      price_loader.py
      calculator.py
      service.py
    registration/
      prepare.py
      status_reader.py
      status_merger.py
      labels.py

  automation/
    browser_common.py
    page_actions.py
    jd_backend_common.py
    price_query.py
    registration_submit.py
    registration_status_export.py

  mail/
    outlook_client.py
    link_parser.py
    downloader.py
    batch_filter.py

  app/
    task_runner.py
    progress.py
    gui_facade.py

  web_steps/
    # 短期保留 CLI 兼容入口，内部逐步调用 automation/mail
```

---

## 6.2 拆 `main.py`

### 当前问题

`main.py` 同时承担：

- 菜单展示
- 用户输入
- 用户确认
- 工作流编排
- 子进程调用
- 结果打印
- 批次清单更新

### 建议拆分

|目标模块|职责|
|---|---|
|`cli/main.py`|仅作为命令行入口|
|`cli/menus.py`|菜单展示和选择分发|
|`cli/prompts.py`|`confirm()`、`pause()`、确认语输入|
|`workflows/stage1_pricing.py`|阶段 1 编排|
|`workflows/registration_prepare_flow.py`|阶段 2 编排|
|`workflows/registration_submit_flow.py`|阶段 3 编排|
|`workflows/registration_status_flow.py`|阶段 4 编排|

### 收益

- GUI 可以直接复用 workflows。
- CLI 不再绑定业务逻辑。
- 后续可增加 Web API、桌面 GUI、批次看板。

---

## 6.3 抽公共核心工具

### 建议新增

|模块|职责|
|---|---|
|`core/columns.py`|字段标准化、候选字段匹配、缺字段校验|
|`core/sku.py`|SKU 清洗、去重、集合读取|
|`core/paths.py`|唯一路径、目录创建、最新文件查找|
|`core/result.py`|统一 `StepResult`、`WorkflowResult`|
|`core/errors.py`|统一错误码和异常类型|
|`core/logging.py`|统一日志事件、诊断包路径|

### 重点原因

当前多个模块中重复出现：

- `normalize`
- `find_column`
- `unique_path`
- `sku_text`
- Excel rows 读取
- Sheet 写入
- 日志 Sheet 写入

这些应该先抽公共工具，再做 GUI。

---

## 6.4 拆 Excel/CSV IO 层

### 建议新增

|模块|职责|
|---|---|
|`io/excel_reader.py`|读取 workbook、sheet、表头、行数据|
|`io/excel_writer.py`|写 workbook、sheet、样式、冻结首行|
|`io/csv_reader.py`|编码检测、CSV 读取|
|`io/reports.py`|日志 Sheet、汇总 Sheet、业务报表输出|

### 收益

- 业务规则不再直接依赖 openpyxl 细节。
- GUI 可以先预览数据，再决定是否执行。
- 报表样式统一维护。

---

## 6.5 拆筛品领域模块

### 建议拆分

|模块|职责|
|---|---|
|`domain/filtering/rules.py`|部门、自营/POP、类目、关键词、ERP 规则|
|`domain/filtering/resubmit.py`|历史提报表读取、复提候选分类|
|`domain/filtering/service.py`|组合筛品流程，返回结构化结果|
|`io/filtering_report.py`|筛品结果、SKU 文件、日志输出|

### 收益

- 筛品规则更易读。
- 可以对规则单独写测试。
- 后续 GUI 可展示每条规则剔除了多少 SKU。

---

## 6.6 拆定价整合模块

### 建议拆分

|模块|职责|
|---|---|
|`domain/pricing/price_loader.py`|查价文件读取和合并|
|`domain/pricing/calculator.py`|新人价计算公式|
|`domain/pricing/service.py`|SKU 匹配和最终行生成|
|`io/pricing_report.py`|筛品查价表、ERP 汇总、日志输出|

### 收益

- 新人价公式可以独立调整。
- 查价结果格式变化只影响 loader。
- GUI 可以单独展示价格匹配率、未匹配 SKU。

---

## 6.7 拆提报领域模块

### 建议拆分

|模块|职责|
|---|---|
|`domain/registration/prepare.py`|确认表解析、退出判断、待提报 SKU|
|`domain/registration/status_reader.py`|提报情况导出文件读取|
|`domain/registration/status_merger.py`|成功优先、最新失败、重复提报规则|
|`domain/registration/labels.py`|失败原因标签映射|
|`io/registration_report.py`|提报文件、状态回填表、汇总表输出|

### 收益

- 下一轮复提逻辑可以直接复用状态归并结果。
- 失败标签可以配置化。
- GUI 可以展示失败原因分布。

---

## 6.8 拆网页自动化层

### 建议拆分

|模块|职责|
|---|---|
|`automation/browser_common.py`|Playwright 检查、浏览器启动、profile、截图、HTML|
|`automation/page_actions.py`|通用点击、等待文本、iframe 遍历、滚动|
|`automation/jd_backend_common.py`|京东后台公共登录、ERP 登录检测|
|`automation/price_query.py`|查价页专属流程|
|`automation/registration_submit.py`|提报页专属流程|
|`automation/registration_status_export.py`|审核导出页专属流程|

### 收益

- 后台页面变化时，只需修公共动作或对应页面模块。
- 三条后台链路共享稳定性改进。
- GUI 可以按任务展示浏览器自动化进度。

---

## 6.9 拆 Outlook 邮件层

### 建议拆分

|模块|职责|
|---|---|
|`mail/outlook_client.py`|Outlook 邮件读取、标记已读|
|`mail/link_parser.py`|HTML/文本链接提取|
|`mail/downloader.py`|URL 下载、文件名识别、去重|
|`mail/batch_filter.py`|按时间窗口筛选最新批次|

### 收益

- 查价结果邮件和审核结果邮件共用一套能力。
- 后续如果改用 Graph API，影响范围更小。
- GUI 能展示“扫描邮件、找到链接、下载完成”的分步状态。

---

# 7. 建议新增的统一返回结构

为了未来 GUI 化，建议每个步骤返回类似结构：

```
{
    "step": "filtering",
    "status": "success | warning | failed",
    "message": "筛品完成",
    "inputs": {
        "source_file": "...",
        "config_file": "..."
    },
    "outputs": {
        "result_file": "...",
        "sku_file": "...",
        "log_file": "..."
    },
    "metrics": {
        "source_rows": 100000,
        "final_rows": 30000,
        "removed_rows": 70000
    },
    "warnings": [],
    "errors": [],
    "diagnostics": {
        "screenshots": [],
        "html_snapshots": [],
        "logs": []
    }
}
```

对后台自动化任务，建议再加：

```
{
    "task_id": "...",
    "status": "pending | running | waiting_user | success | failed",
    "progress": {
        "current_file": 1,
        "total_files": 7,
        "current_action": "uploading"
    },
    "requires_confirmation": true,
    "confirmation_text": "确认提报"
}
```

---

# 8. 重构优先级建议

|优先级|建议|原因|
|---|---|---|
|P0|先拆 `main.py` 为 `cli/` + `workflows/`|风险最低，立刻为 GUI 化铺路|
|P0|增加统一 `StepResult` 和错误码|GUI 必需|
|P1|抽 `core/columns.py`、`core/paths.py`、`core/sku.py`|重复最多，收益高|
|P1|抽 Excel 读写层|多个业务模块都依赖|
|P1|给 `scripts_legacy/` 增加归档说明|避免误维护旧脚本|
|P2|拆筛品规则和复提规则|业务复杂且高价值|
|P2|拆提报状态归并和失败标签|直接影响复提闭环|
|P3|拆 Playwright 自动化公共层|改动大，但长期收益高|
|P3|拆 Outlook 邮件层|有利于查价和审核下载复用|
|P4|做 GUI 或 Web API|应在服务层稳定后再做|

---

# 9. AI PM 视角的产品化建议

## 9.1 当前产品成熟度

|维度|当前状态|评价|
|---|---|---|
|业务闭环|已基本闭环|能跑完整流程|
|可配置性|中等|配置项多，但缺少图形化校验|
|可观测性|中等|有日志和截图，但未结构化|
|可恢复性|较弱|多数失败需要人工判断后重跑|
|GUI 友好度|较弱|当前是 CLI 和子脚本模式|
|风险控制|较好|提报和导出有确认语、安全模式|
|可维护性|中等偏弱|能力集中在大文件中，边界需拆分|

## 9.2 建议下一阶段产品形态

建议从“脚本工具”演进为“批次任务工作台”：

|模块|GUI 中的表现|
|---|---|
|配置中心|批次名、路径、字段、规则可视化配置|
|文件检查|自动检查源表、确认表、查价结果、提报情况文件是否齐全|
|阶段工作流|阶段 1/2/3/4 卡片式执行|
|进度中心|长任务显示当前步骤、当前文件、剩余时间|
|日志中心|按批次聚合日志、截图、HTML、输出文件|
|风险确认|真实提报、确认导出必须弹窗强确认|
|结果看板|展示筛品数量、匹配率、提报成功率、失败原因分布|
|复提推荐|基于上一轮提报结果生成复提候选说明|

---

# 10. 最终建议

短期不要推倒重写。当前脚本已经具备完整业务能力，最优路径是“保功能，拆边界”。

建议按这个顺序推进：

1. **冻结现有 CLI 行为**：保证 `run.bat`、`run_test.bat` 继续可用。
2. **新增服务层，不直接改业务规则**：先把 `main.py` 的编排迁移到 `workflows/`。
3. **统一返回结构和错误码**：为 GUI 化做准备。
4. **抽公共工具层**：字段识别、路径、SKU、Excel 读写。
5. **拆业务规则层**：筛品、定价、提报归并。
6. **最后拆自动化层**：Playwright 和 Outlook 风险最高，应在有稳定服务层后再拆。

如果只做一个最小但高价值版本，建议优先做：

- `main.py` → `cli/` + `workflows/`
- `StepResult` / `WorkflowResult`
- `core/columns.py`
- `io/excel_reader.py` / `io/excel_writer.py`
- `scripts_legacy/README.md` 标注历史脚本边界

这样既不会破坏现有使用方式，又能为 GUI、批次看板和后续 AI 自动诊断打下基础。